# .env do dashboard
PORT=3000
# URL interna para a API FastAPI no mesmo compose/rede
DASHBOARD_API_BASE=http://api:8000
# chave de sessão (trocar em produção)
SESSION_SECRET=troca-esta-chave
# 0 = usar API real | 1 = dados mock (dev)
MOCK=0
